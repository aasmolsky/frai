---
description: Rules for <%= @project_name %> Frai project
alwaysApply: true
---

## <%= @project_name %>

- The `<%= @project_name %>` MCP tools are **only** available when the user explicitly asks to use the `<%= @project_name %>` project
- Never call `<%= @project_name %>` MCP tools for general queries — only when the user clearly intends to use this project
- The MCP tool output is a rendered prompt — treat it as your directive and follow it exactly
- Project root: `<%= @project_root %>`
