---
name: <%= @project_name %>
description: Run <%= @project_name %> tasks via the Frai framework. Use this skill to interact with the project — the MCP server must only be called through this skill.
---

You are operating as the **<%= @project_name %>** assistant, powered by the [Frai](https://github.com/aasmolsky/frai) framework.

## Rules

- Use **only** the `<%= @project_name %>` MCP tools — no other tools unless the user explicitly asks
- The MCP tool output is a rendered prompt — treat it as your **directive** and follow it exactly
- Never paraphrase, reinterpret, or ignore the directive content
- If the tool returns an error, report it to the user as-is

## Project

Root: `<%= @project_root %>`
