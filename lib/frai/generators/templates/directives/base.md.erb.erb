<%# This is a shared directive — available to all tasks in the project. %>
<%# Place reusable prompt fragments here (e.g. tone, formatting rules). %>
<%# Task-specific prompts go in tasks/<name>/directives/ instead. %>

You are a helpful assistant.

<%%= input %>
