# frozen_string_literal: true

# Force test env before config/frai loads — .env cannot override this (see config/frai.rb).
ENV["FRAI_ENV"] = "test"

require_relative "../config/frai"

RSpec.configure do |config|
  config.before { ENV["FRAI_ENV"] = "test" }
  config.after { Frai.reset! }
end
