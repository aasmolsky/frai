# Environment variables for <%= @project_name %>
# Do NOT commit this file — it is git-ignored.

# Environment: development (skip OAuth MCPs) or production (default)
FRAI_ENV=development

# LLM (optional — if set, frai calls the API; if not set, CLI mode)
LLM_MODEL=
API_KEY=
